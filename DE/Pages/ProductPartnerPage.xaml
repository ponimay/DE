<Page x:Class="DE.Pages.ProductPartnerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DE.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductPartnerPage">

    <Grid>
        <ListView d:ItemsSource="{d:SampleData ItemCount=5}" Name="LV" HorizontalContentAlignment="Stretch" Grid.Row="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <StackPanel>
                            <DockPanel>
                                <TextBlock Text="{Binding Product.ProductTitle}" Margin="0 5"/>

                            </DockPanel>
                            <TextBlock Text="{Binding Product.ProductCount}" Margin="0 5"/>
                            <TextBlock Text="{Binding Product.ProductMinPrice}" Margin="0 5"/>

                        </StackPanel>

                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
